[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "md2beauty"
version = "0.1.0"
description = "Convert Markdown to HTML, DOCX, PPTX, PDF with theme and extension support."
authors = [
    { name="Adrien Nelis", email="your@email.com" }
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "python-docx",
    "python-pptx",
    "pydantic>=2.0.0",
    "Pygments>=2.15.0"
]

[project.scripts]
md2beauty = "md2beauty.cli:main"

[tool.pytest.ini_options]
addopts = "-q --cov=md2beauty --cov-report=term-missing --cov-report=xml:coverage.xml"
testpaths = ["tests"]
filterwarnings = [
    "ignore::DeprecationWarning",
]

[tool.coverage.run]
branch = true
source = ["md2beauty"]
omit = [
    "md2beauty/__main__.py",
]

[tool.coverage.report]
fail_under = 50
show_missing = true
