# Sample: Code & Pre

Inline `code` example and a JS fenced code block plus a preformatted chunk:

```js
console.log('hello world');
```

    # preformatted (indented) block
    for i in range(3):
        print(i)
